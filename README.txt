Nebras project template.

Replace OPENAI_API_KEY in .env.
Install: pip install -r requirements.txt
Run: python app.py

This template is a starting point. Your existing app.py can be migrated into this structure.
